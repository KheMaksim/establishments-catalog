import { Box, Typography } from "@mui/material";

const AnonymousForm = () => {
    return (
        <Box>
            <Typography variant="h6" fontWeight={700} textAlign={"center"}>
                Sign in before leave a review.
            </Typography>
        </Box>
    );
};

export default AnonymousForm;
